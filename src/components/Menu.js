import React from 'react';
import { directive } from '@babel/types';

const Menu = () => {
    return (
        <nav class="navbar navbar-light bg-light">
            <span class="navbar-brand mb-0 h1">Song Loader</span>
        </nav>
      );
}
 
export default Menu;